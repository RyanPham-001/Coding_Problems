import math
series_limit = 0
start = 1
while(series_limit<9999):
  start += 1/(factorial(series_limit))
  
def factorial (value):
  if (value ==1):
    return(1)
  else:
   final *= value
   value--
   factorial(value)
return(final)
  
  
